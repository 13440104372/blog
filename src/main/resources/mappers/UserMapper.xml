<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zbw.blog.module.user.UserMapper">

    <select id="getUserPermissions" resultType="permission">
        select p.*
        from user u
                 left join user_role_relation urr on u.id = urr.user_id
                 left join role_permissions_relation rpr on urr.role_id = rpr.role_id
                 left join permission p on rpr.authority_id = p.id
        where u.id = #{userId}
    </select>
</mapper>